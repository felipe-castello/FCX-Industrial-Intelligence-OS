server {
  listen 80;
  server_name app.nexusiotenergy.com.br;
  location / { proxy_pass http://localhost:5173; }
}
server {
  listen 80;
  server_name api.nexusiotenergy.com.br;
  location / { proxy_pass http://localhost:3000; }
}
server {
  listen 80;
  server_name grafana.nexusiotenergy.com.br;
  location / { proxy_pass http://localhost:3001; }
}
